//! Serum / OpenBook CLOB venue adapter — stub for future SDK integration.
//!
//! The live query logic currently lives in [`crate::sor::router`].
//! Move the `query_serum` function here and make it `pub` once the
//! `serum-dex` or `openbook-client` dependency is added to `Cargo.toml`.
