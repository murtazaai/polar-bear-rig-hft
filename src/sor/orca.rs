//! Orca Whirlpool venue adapter — stub for future SDK integration.
//!
//! The live query logic currently lives in [`crate::sor::router`].
//! Move the `query_orca` function here and make it `pub` once the
//! `orca-whirlpools-sdk` dependency is added to `Cargo.toml`.
